package model.enums;

public enum Faction {
    GER,    // Było GERMANY, musi być GER (bo tak jest w CSV i Main)
    USA,
    JAP,    // Było JAPAN, musi być JAP
    ZSR     // Było ZSRR, musi być ZSR (tak jest w Twoim pliku ships.csv)
}